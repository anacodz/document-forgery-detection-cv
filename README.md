# document-forgery-detection-cv
Deep learning binary classifier for document forgery detection using transfer learning (ResNet18) and synthetic data augmentation. Demonstrates end-to-end ML pipeline with PyTorch for fraud prevention applications.
